import type { Metadata } from "next";
import { Inter } from "next/font/google";
import "./globals.css";

const inter = Inter({
  subsets: ["latin"],
  variable: "--font-inter",
});

export const metadata: Metadata = {
  title: "Divine Vision Supplies Ltd | Building Materials & Hardware",
  description: "Divine Vision Supplies Ltd. Building materials, hardware, mabati, cement, tools. Fast delivery Kenya-wide. WhatsApp to order.",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body className={`${inter.variable} font-sans antialiased`}>
        {children}
      </body>
    </html>
  );
} 